import React from 'react';
const UserDashboard = ({ currentUser, selectedEmployee, tasks, setTasks, handleLogout, chatMessages, setChatMessages, newMessage, setNewMessage, departments, knowledgeBase }) => { return <div>UserDashboard Component</div>; };
export default UserDashboard;